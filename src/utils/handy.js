export function onKeyUpAmount(event) {
	// console.log('onKeyUpAmount')
	// let val = event.target.value;
	// if (isNaN(val)) {
	// 	val = val.replace(/[^0-9.]/g, '');
	// 	if (val.split('.').length > 2) val = val.replace(/\.+$/, '');
	// }
	// event.target.value = val;
}
